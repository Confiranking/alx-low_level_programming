#include <stdio.h>

/**
 * main - prints alpha in lower case
 * Returning; Always 0
 */
int main(void)
{
	char lc;

	for (lc = 'a'; lc <= 'z'; lc * *)
	{
		putchar(lc);
	}
	putcher('\n');
	return (0);
}
